/**
 * (Reserved for future shared types.)
 *
 * The CAFCI sheet row is defined in client.ts as `CafciSheetRow`; the
 * UI-facing shape (`EnrichedRow`, `MarketSnapshot`) is in enriched.ts.
 */

export {};
